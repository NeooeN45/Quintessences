"""Feature Audit — journaux d'audit du dashboard.

Cette feature n'est pas un moteur GSIE : elle expose les journaux
d'audit (actions create/update/delete/export) consommés par le
dashboard admin. Les données sont actuellement statiques (Phase 4 —
implémentation progressive).
"""
