# CONTEXT_SNAPSHOT_001 — (réservé)

Livrable : 012 — Mémoire du projet et snapshots
Statut : Draft — en attente du 10e Directive (non atteint)

## Objet

Ce fichier est un **snapshot de contexte** : une photographie figée de l'état
du projet à un instant déterminé, destinée à permettre à un lecteur (humain ou
agent) de reconstituer le contexte complet sans dépendre de l'historique vivant.

## Déclenchement prévu

Par conception, ce premier snapshot est **prévu pour être figé à la 10e
Directive** (cf. index de la mémoire dans `22_PROJECT_MEMORY/PROJECT_MEMORY.md`).
À la date courante, le projet compte les directives actives `GSIE-DIR-0001`,
`GSIE-DIR-0003` et `GSIE-DIR-0004` : le seuil n'est pas atteint.

## État d'attente

Le snapshot reste donc volontairement vide de contenu figé. La vue courante de
l'état du projet est maintenue dans `PROJECT_MEMORY.md` (racine et
`22_PROJECT_MEMORY/`). Ce fichier n'est pas un oubli : il est **en attente de
son déclencheur**.

> **Note** : le snapshot sera déclenché à la 10e Directive. Tant que ce seuil
> n'est pas atteint, ce fichier reste en statut `Draft` et vide de contenu figé.

> À compléter le moment venu : phase, directive courante, livrables et leurs
> statuts, décisions actives, vision courante, risques ouverts.
