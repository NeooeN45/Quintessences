# OS
Thumbs.db
Desktop.ini
.DS_Store

# Temp
.commit-msg.txt

# Python (tools/)
__pycache__/
*.pyc
.pytest_cache/

# Éditeurs
.vscode/
.idea/
.obsidian/
*.swp
*~

# Claude Code — config d'outil IA locale (non versionnée)
.claude/
CLAUDE.md

# Devin CLI — config locale (clés personnelles, non versionnée)
.devin/config.local.json

# Secrets — fichiers d'environnement réels (ne jamais committer)
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Temporaire
tmp/
*.tmp
*.log

# Scripts temporaires à la racine
/_tmp_*.py
/_tmp_*.sh

# Rust — build artifacts et venv maturin
**/target/
**/.venv/

# Node (si outillage ajouté plus tard)
node_modules/

# Archives binaires (les packs contexte agent ne sont pas versionnés)
*.zip
.coverage
GSIE/TOOLS/dataset_forge/
apps/Ignis/git_init.sh
apps/Ignis/run_premier_vol.sh
apps/Ignis/run_px4.sh

# Données brutes Météo-France (scratch, non versionnées — 18MB+)
GSIE/API/scratch_synop/

# Sorties de travail du harnais de mutation et échantillons de mise au point.
# Elles portent l'état d'une exécution, pas une décision : les versionner
# ferait passer un instantané de poste de travail pour un résultat de projet.
GSIE/API/harnais_*.txt
GSIE/API/climate_sample.json
GSIE/API/knowledge_stats.json

# Repos externes (repos git indépendants dans la structure Quintessences)
# GeoSylva — repo GitHub NeooeN45/GeoSylva
apps/GeoSylva/
# QGISIA — repo GitHub NeooeN45/QGISIAPRO
apps/QGISIA/
# Forge — usine de données (pas de remote configuré)
Forge/
# Treekipedia — clone d'inspection du dépôt MIT (porte son propre .git).
# Copie de travail servant l'analyse RFC-0030 ; la source de vérité reste le
# dépôt amont. Vendorer le clone dans Quintessences en ferait un gitlink cassé
# et masquerait sa licence d'origine.
21_EXPERIMENTS/_treekipedia_inspection/

# Vault Obsidian personnel de navigation (NON canonique — cf. CHANGELOG 2026-07-13).
# La source de vérité de la gouvernance vit dans les dossiers numérotés
# (00_CONSTITUTION/, 03_DECISIONS/, etc.), jamais dans ce vault.
# Ancré à la racine pour ne matcher que le vault, pas un futur dossier homonyme.
/Quintessences/
GSIE/API/backups/
